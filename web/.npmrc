# react-simple-maps@3 has an outdated peer range that excludes React 19.
# The application uses Next.js 16 and React 19, so do not fail installs solely
# because of that peer-dependency declaration.
legacy-peer-deps=true
